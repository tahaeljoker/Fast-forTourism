# دليل GitHub الكامل - الإعداد والمشاركة والتشغيل

## 1️⃣ الخطوة الأولى: إعداد GitHub ورفع المشروع

### أ) إنشاء مستودع جديد على GitHub
1. اذهب إلى [GitHub.com](https://github.com)
2. سجل دخولك أو أنشئ حساب جديد
3. اضغط على **+** في الزاوية العلوية اليمنى واختر **New repository**
4. ملء البيانات:
   - **Repository name**: `Fast-forTourism`
   - **Description**: `Tourism Management Platform with Multi-language Support`
   - اختر **Public** أو **Private** حسب احتياجك
5. **لا تختر** Initialize with README (عندنا واحد بالفعل)
6. اضغط **Create repository**

### ب) ربط المستودع المحلي بـ GitHub

بعد إنشاء المستودع، سيظهر لك أوامر Git. نفذ هذه الأوامر:

```bash
# إذا كنت في المجلد بالفعل
cd d:\programming\projects\work\Fast-forTourism

# أضف الـ remote اللي على GitHub
# استبدل YOUR_USERNAME باسم المستخدم الخاص بك
git remote add origin https://github.com/YOUR_USERNAME/Fast-forTourism.git

# أو استخدم SSH إذا كان مكعد (أسهل بدون password):
git remote add origin git@github.com:YOUR_USERNAME/Fast-forTourism.git

# تأكد من الـ remote
git remote -v

# أرسل التحديثات (أول مرة)
git branch -M main
git push -u origin main
```

## 2️⃣ كيفية مشاركة المشروع مع مبرمج آخر

### الطريقة الأولى: المشاركة المباشرة (للفريق الصغير)

#### من طرفك (المالك):
1. اذهب إلى صفحة المستودع على GitHub
2. اضغط **Settings** → **Collaborators** 
3. اضغط **Add people**
4. اكتب اسم المستخدم أو البريد الإلكتروني للمبرمج الآخر
5. اختر مستوى الصلاحية:
   - **Pull only**: يمكنه التحميل فقط
   - **Triage**: مراقب ومراجع
   - **Push**: يمكنه الرفع والتعديل
   - **Maintain**: إدارة كاملة
   - **Admin**: تحكم كامل

#### من طرف المبرمج الآخر:
سيحصل على دعوة عبر البريد الإلكتروني ويقبلها

### الطريقة الثانية: Fork و Pull Request (للمشاريع الكبيرة)
- المبرمج ينشئ نسخة خاصة به (Fork)
- يعدل الكود في نسخته
- يرسل Pull Request لك لمراجعة التغييرات

### الطريقة الثالثة: نسخة مباشرة (الأسهل)
شارك رابط المستودع مع المبرمج:
```
https://github.com/YOUR_USERNAME/Fast-forTourism
```

## 3️⃣ كيف يشغل المبرمج الآخر المشروع؟

### الخطوة 1: تحميل المشروع (Clone)

```bash
# انسخ المستودع
git clone https://github.com/YOUR_USERNAME/Fast-forTourism.git

# ادخل المجلد
cd Fast-forTourism
```

### الخطوة 2: تثبيت المتطلبات

```bash
# ادخل مجلد التطبيق
cd app

# ثبت جميع المكتبات
npm install
# أو استخدم
yarn install
# أو
pnpm install
```

### الخطوة 3: إعداد المتغيرات البيئية

في المجلد `app`، أنشئ ملف `.env.local`:

```bash
# .env.local
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_LANGUAGE=ar
```

### الخطوة 4: تشغيل المشروع

```bash
# الوضع التطويري
npm run dev
# أو
yarn dev

# سيظهر رابط مثل:
# ✓ Ready in 2.5s
# ➜  http://localhost:3000
```

### الخطوة 5: فتح المتصفح

اذهب إلى: `http://localhost:3000`

---

## 📋 جدول أوامر Git المهمة للتعاون

```bash
# تحديث الكود من GitHub
git pull origin main

# إنشاء فرع جديد للعمل
git checkout -b feature/اسم-الميزة

# حفظ التغييرات محليًا
git add .
git commit -m "وصف التغييرات"

# رفع الفرع الجديد
git push origin feature/اسم-الميزة

# دمج الفرع مع الفرع الرئيسي
git checkout main
git merge feature/اسم-الميزة
git push origin main

# حذف فرع محلي
git branch -d feature/اسم-الميزة

# حذف فرع بعيد
git push origin --delete feature/اسم-الميزة
```

---

## 🔐 أفضل الممارسات عند العمل بفريق

### 1. استخدم فروع (Branches)
```bash
# لا تعمل على main مباشرة
# أنشئ فرع لكل ميزة
git checkout -b feature/navbar-update
```

### 2. الرسائل الواضحة
```bash
# ✅ جيد
git commit -m "Update navbar styling and fix mobile responsiveness"

# ❌ سيء
git commit -m "updates"
```

### 3. اسحب قبل ما تدفع (Pull before Push)
```bash
git pull origin main
git push origin feature/my-feature
```

### 4. مراجعة الكود (Code Review)
- استخدم Pull Requests
- اطلب مراجعة من الآخرين قبل الدمج

---

## 🚀 سريع المراجعة للبدء:

| الخطوة | الأمر |
|------|------|
| إعداد Git | `git config --global user.name "أسمك"` |
| | `git config --global user.email "بريدك@gmail.com"` |
| تحميل المشروع | `git clone [الرابط]` |
| تثبيت المكتبات | `cd app && npm install` |
| البدء | `npm run dev` |
| حفظ التغييرات | `git add . && git commit -m "وصف"` |
| الرفع | `git push origin main` |

---

## ❓ أسئلة شائعة

**س: ماذا لو حصل conflict عند دمج الفروع؟**
ج: هذا طبيعي. افتح الملفات بـ VS Code وحل التعارضات يدويًا، ثم احفظ وارفع.

**س: كيف أرجع commit سابق؟**
ج: `git revert [commit-hash]` أو `git reset --hard [commit-hash]`

**س: هل أحتاج SSH أم HTTPS؟**
ج: HTTPS أسهل في البداية. SSH أفضل لاحقًا لتجنب إدخال password.

**س: المشروع يشتغل عندي بس ما يشتغل عند الآخر؟**
ج: تأكد من:
- نسخة Node.js متطابقة (`node --version`)
- ملف `.env.local` موجود بالإعدادات الصحيحة
- جميع المكتبات مثبتة (`npm install`)

---

## 📞 الدعم والمساعدة

- توثيق GitHub: https://docs.github.com
- مساعدة Next.js: https://nextjs.org/docs
- سؤال في Stack Overflow بـ tag `git` و `github`

---

✅ الآن أنت مستعد للعمل مع فريق على GitHub!
