# 🚀 دليل النشر على Vercel - Vercel Deployment Guide

## إعدادات Vercel الحالية

- **Repository**: https://github.com/tahaeljoker/Fast-forTourism
- **Framework**: Next.js
- **Root Directory**: app/
- **Deployment**: Automatic (CI/CD)

---

## ✅ الإعداد الأول (مرة واحدة فقط)

### 1. ربط المستودع بـ Vercel

```bash
# الخيار 1: استخدم Vercel CLI
npm i -g vercel
vercel link

# الخيار 2: عبر الواجهة
# اذهب إلى https://vercel.com/dashboard
# اضغط "New Project"
# اختر المستودع Fast-forTourism من GitHub
# اختر اسم المشروع
# اضغط "Deploy"
```

### 2. إضافة متغيرات البيئة على Vercel

في لوحة تحكم Vercel:

1. اذهب إلى **Settings** → **Environment Variables**
2. أضف المتغيرات:

```
NEXT_PUBLIC_API_URL = http://localhost:3000 (للتطوير)
NEXT_PUBLIC_LANGUAGE = ar
```

### 3. تفعيل النشر التلقائي (Auto Deploy)

Vercel يفعل هذا افتراضيًا:
- كل push إلى `main` → نشر على الإنتاج
- كل push إلى فرع آخر → نشر على preview

---

## 📝 سير العمل اليومي

### الطريقة الأولى: استخدام الـ Batch File

```bash
# في Windows PowerShell أو Command Prompt
.\update_and_deploy.bat

# سيطلب منك إدخال رسالة الالتزام
# ثم يرفع كل شيء تلقائيًا
```

### الطريقة الثانية: استخدام أوامر Git يدويًا

```bash
# 1. اسحب آخر التحديثات
git pull origin main

# 2. أضف التغييرات
git add .

# 3. احفظ التغييرات
git commit -m "Update: وصف التغييرات"

# 4. ارفع إلى GitHub
git push origin main

# 5. Vercel ينشر تلقائيًا!
```

### الطريقة الثالثة: استخدام Vercel CLI

```bash
# بناء محلي للاختبار
vercel build

# نشر preview
vercel deploy

# نشر على الإنتاج
vercel --prod
```

---

## 🔄 دورة النشر الكاملة

```
┌─────────────────────────────────────────┐
│ 1. تعديل الكود محليًا                  │
│    (في مجلد app/)                      │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 2. اختبر المشروع محليًا                │
│    npm run dev                          │
│    http://localhost:3000                │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 3. رفع التحديثات إلى GitHub            │
│    .\update_and_deploy.bat              │
│    أو: git add . && git commit && push  │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 4. Vercel يكتشف التحديثات تلقائيًا    │
│    ويبدأ النشر                          │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 5. تحقق من الحالة على Vercel           │
│    https://vercel.com/dashboard         │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 6. الموقع يعمل على الإنتاج! 🎉         │
│    https://fast-fortourism.vercel.app   │
└─────────────────────────────────────────┘
```

---

## 📊 معلومات Vercel المهمة

### رابط لوحة التحكم:
```
https://vercel.com/dashboard
```

### رابط المشروع:
```
https://vercel.com/tahaeljoker/fast-fortourism
```

### رابط الموقع المباشر:
```
https://fast-fortourism.vercel.app
```

### رابط Preview للفروع:
```
https://[branch-name].fast-fortourism.vercel.app
```

---

## 🔍 مراقبة النشر

### عبر لوحة Vercel:

1. اذهب إلى: https://vercel.com/dashboard
2. اختر المشروع: Fast-forTourism
3. شاهد حالة النشر:
   - 🔵 **Running** - قيد التشغيل
   - ✅ **Ready** - تم النشر بنجاح
   - ❌ **Failed** - فشل النشر

### عبر Git:

```bash
# شاهد آخر commits
git log --oneline -5

# شاهد الـ push logs
git push origin main --verbose

# تحقق من حالة الفرع الحالي
git branch -v
```

---

## ⚙️ ملفات الإعدادات

### vercel.json
```json
{
  "buildCommand": "cd app && npm run build",
  "devCommand": "cd app && npm run dev",
  "installCommand": "cd app && npm install",
  "framework": "nextjs",
  "projectSettings": {
    "rootDirectory": "app"
  }
}
```

### .vercelignore
```
# الملفات التي لا تُرفع لـ Vercel
node_modules
.env.local
.git
*.md
```

---

## 🆘 حل المشاكل الشائعة

### ❌ "Build failed on Vercel"

**الحل:**
```bash
# 1. تحقق محليًا
cd app
npm run build

# 2. إذا حصل خطأ:
npm install
npm run build

# 3. إذا لم يحل:
rm -rf node_modules .next
npm install
npm run build
```

### ❌ "Preview deployment failed"

**الحل:**
```bash
# تأكد من أن package.json موجود في app/
# وأن جميع dependencies مثبتة
cd app
npm install --save
git push origin main
```

### ❌ "Environment variables missing"

**الحل:**
1. اذهب إلى Vercel Dashboard
2. Settings → Environment Variables
3. أضف المتغيرات المفقودة
4. أعد النشر (Redeploy)

### ❌ "Port already in use"

**الحل:**
```bash
# استخدم port مختلف
npm run dev -- -p 3001
```

---

## 📋 قائمة التحقق قبل النشر

- [ ] تم اختبار الموقع محليًا (`npm run dev`)
- [ ] لا توجد أخطاء في console
- [ ] جميع الصور والملفات الثابتة موجودة
- [ ] المتغيرات البيئية معرّفة بشكل صحيح
- [ ] تم حفظ جميع التغييرات (`git add .`)
- [ ] رسالة الالتزام واضحة وموجزة
- [ ] تم السحب من main قبل الدفع (`git pull`)

---

## 🚀 النشر السريع (One-Click Deploy)

### استخدام Batch File:

```bash
.\update_and_deploy.bat
```

هذا الملف يفعل:
1. ✅ سحب آخر التحديثات من GitHub
2. ✅ إضافة كل التغييرات
3. ✅ إنشاء التزام مع رسالة
4. ✅ دفع كل شيء إلى GitHub
5. ✅ Vercel يكتشف ويشر تلقائيًا

---

## 📞 نصائح مهمة

💡 **أفضل الممارسات:**
- استخدم فروع منفصلة للميزات الجديدة
- اختبر دائمًا قبل الدفع
- اكتب رسائل التزام واضحة
- راقب لوحة Vercel بعد الدفع

💡 **لضمان نشر سلس:**
- تأكد من أن `app/` هو مجلد Next.js الرئيسي
- تأكد من وجود `package.json` في `app/`
- لا تضع `.env.local` في Git (موجود `.env.example`)
- اختبر `npm run build` محليًا قبل الدفع

---

## 🔐 الأمان

### متغيرات البيئة الحساسة:

**لا تضع في Git:**
```
❌ API keys
❌ Database passwords
❌ Secret tokens
❌ .env.local
```

**ضعها في Vercel Dashboard:**
1. Settings → Environment Variables
2. أضف المتغيرات الحساسة هناك

**استخدم `.env.example` للمثال:**
```
NEXT_PUBLIC_API_URL=http://localhost:3000
```

---

## 📈 الأداء والمراقبة

### شاهد أداء الموقع:

1. اذهب إلى Vercel Dashboard
2. اختر المشروع
3. اذهب إلى **Analytics**
4. شاهد:
   - Response times
   - Error rates
   - Traffic

---

## ✅ الخلاصة

**الآن لديك:**

✅ مستودع GitHub متصل  
✅ Vercel مُعد للنشر التلقائي  
✅ Batch file للنشر السريع  
✅ معلومات كاملة عن النشر  

**كل ما تحتاجه للنشر على Vercel! 🚀**

---

### آخر تحديث: 7 فبراير 2026
### الحالة: ✅ جاهز للنشر
