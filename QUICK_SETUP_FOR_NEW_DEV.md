# 🚀 دليل الإعداد السريع للمبرمج الجديد

## للبدء في 5 دقائق فقط ⏱️

### الخطوة 1️⃣: تنزيل المشروع
```bash
git clone https://github.com/YOUR_USERNAME/Fast-forTourism.git
cd Fast-forTourism
```

### الخطوة 2️⃣: تثبيت المتطلبات
```bash
cd app
npm install
```

### الخطوة 3️⃣: إعداد البيئة
أنشئ ملف `app/.env.local` وأضف:
```
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_LANGUAGE=ar
```

### الخطوة 4️⃣: التشغيل
```bash
npm run dev
```

### الخطوة 5️⃣: افتح المتصفح
```
http://localhost:3000
```

---

## ✅ المتطلبات المسبقة

تأكد من تثبيت هذه على جهازك:

- **Node.js** (v18 أو أحدث)  
  تحقق: `node --version`

- **npm** أو **yarn** أو **pnpm**  
  تحقق: `npm --version`

- **Git**  
  تحقق: `git --version`

- **VS Code** (اختياري ولكن موصى به)

---

## 📂 هيكل المشروع

```
Fast-forTourism/
├── app/                    # مجلد التطبيق الرئيسي (Next.js)
│   ├── src/
│   │   ├── app/           # صفحات التطبيق
│   │   ├── components/    # مكونات React
│   │   ├── lib/           # دوال مساعدة
│   │   ├── config/        # إعدادات
│   │   ├── context/       # React Context
│   │   ├── data/          # بيانات ثابتة
│   │   └── types/         # تعريفات TypeScript
│   ├── public/            # ملفات عامة (صور، أيقونات)
│   └── package.json       # المكتبات والنصوص
├── README.md              # معلومات المشروع
└── GITHUB_SETUP_AND_SHARING.md  # دليل GitHub كامل
```

---

## 🔧 أوامر مفيدة

```bash
# فتح المشروع في VS Code
code .

# بناء المشروع
npm run build

# اختبار الأداء
npm run lint

# تنظيف المجلدات المؤقتة
rm -rf node_modules
npm install
```

---

## 🐛 حل المشاكل الشائعة

### ❌ الخطأ: "Port 3000 already in use"
```bash
# استخدم port مختلف
npm run dev -- -p 3001
```

### ❌ الخطأ: "npm command not found"
- أعد تثبيت Node.js من: https://nodejs.org/

### ❌ المشروع بطيء جدًا
```bash
# امسح المجلد والملفات المؤقتة
rm -rf .next
npm run dev
```

### ❌ الملفات لم تتحديث
```bash
# امسح cache المتصفح (Ctrl+Shift+Delete)
# أو استخدم
npm run dev -- --experimental-app
```

---

## 👥 التعاون مع الفريق

### سحب آخر التحديثات
```bash
git pull origin main
```

### إنشاء فرع للعمل عليه
```bash
git checkout -b feature/اسم-الميزة
```

### حفظ التغييرات
```bash
git add .
git commit -m "وصف التغييرات"
git push origin feature/اسم-الميزة
```

### إرسال تغييراتك (Pull Request)
1. اذهب إلى GitHub
2. ستجد زر "Compare & pull request"
3. أضف وصفًا لتغييراتك
4. اضغط "Create pull request"

---

## 📚 المستندات الإضافية

- [دليل GitHub الكامل](./GITHUB_SETUP_AND_SHARING.md)
- [README الرئيسي](./README.md)
- [ملخص التغييرات](./IMPLEMENTATION_SUMMARY.md)

---

## ❓ تحتاج مساعدة؟

1. تحقق من الأخطاء في المحطة (Terminal)
2. اقرأ الملفات في مجلد `DOCUMENTATION_INDEX.md`
3. اسأل أحد أعضاء الفريق

---

**🎉 تم! الآن أنت مستعد للعمل على المشروع!**
