# Environment Configuration Example
# Copy this file to .env.local and fill in the actual values

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000

# Language Configuration (ar for Arabic, en for English)
NEXT_PUBLIC_LANGUAGE=ar

# Admin Panel Settings (if needed)
ADMIN_SECRET_KEY=your_secret_key_here

# Database Configuration (if using external DB)
# DATABASE_URL=your_database_url

# Authentication (if using JWT or OAuth)
# NEXT_AUTH_SECRET=your_auth_secret
# NEXT_AUTH_URL=http://localhost:3000

# Email Configuration (for contact form)
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_password
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# Analytics (optional)
# GOOGLE_ANALYTICS_ID=your_ga_id

# External Services
# WHATSAPP_API_KEY=your_whatsapp_key
# PAYMENT_GATEWAY_KEY=your_payment_key
