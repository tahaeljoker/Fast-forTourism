# 👥 سير العمل الجماعي - Team Workflow

## 📋 نظام إدارة الفريق

هذا المستند يشرح كيفية التعاون الفعال بين أعضاء الفريق على هذا المشروع.

---

## 🔄 دورة حياة المشروع (Development Lifecycle)

```
┌─────────────────────────────────────────────────────────────┐
│  1. اسحب آخر التحديثات من GitHub                          │
│     git pull origin main                                     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  2. أنشئ فرع جديد للميزة أو الإصلاح                       │
│     git checkout -b feature/اسم-الميزة                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  3. اعمل على الكود في الفرع الخاص بك                      │
│     (تعديل الملفات، الاختبار، التصحيح)                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  4. احفظ التغييرات محليًا                                 │
│     git add .                                                │
│     git commit -m "وصف واضح للتغييرات"                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  5. ارفع الفرع إلى GitHub                                 │
│     git push origin feature/اسم-الميزة                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  6. أنشئ Pull Request (PR) على GitHub                      │
│     - اذهب إلى المستودع                                   │
│     - ستجد "Compare & pull request"                        │
│     - أضف وصفًا تفصيليًا                                  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  7. مراجعة الكود (Code Review)                             │
│     - عضو آخر يراجع التغييرات                             │
│     - قد يطلب تعديلات                                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  8. دمج (Merge) مع الفرع الرئيسي                          │
│     - اضغط "Merge pull request" على GitHub                │
│     - احذف الفرع بعد الدمج                                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 🌿 أنواع الفروع (Branch Types)

استخدم هذه الأنماط عند إنشاء فروع جديدة:

| نوع الفرع | الاستخدام | مثال |
|----------|---------|------|
| `feature/` | ميزة جديدة | `feature/navbar-redesign` |
| `bugfix/` | إصلاح خطأ | `bugfix/fix-mobile-responsiveness` |
| `hotfix/` | إصلاح حرج في الإنتاج | `hotfix/critical-payment-error` |
| `docs/` | تحديث التوثيق | `docs/update-api-reference` |
| `refactor/` | تحسين الكود | `refactor/optimize-performance` |
| `test/` | إضافة اختبارات | `test/add-unit-tests` |

**مثال:**
```bash
# ميزة جديدة
git checkout -b feature/add-tour-filters

# إصلاح خطأ
git checkout -b bugfix/fix-form-validation

# تحديث التوثيق
git checkout -b docs/add-setup-guide
```

---

## 💬 معايير رسائل الالتزام (Commit Messages)

اكتب رسائل واضحة وموجزة:

```bash
# ✅ صحيح - واضح ومحدد
git commit -m "Update navbar styling for mobile devices"
git commit -m "Fix form validation error on contact page"
git commit -m "Add language switcher to header"

# ❌ خاطئ - غير واضح
git commit -m "updates"
git commit -m "fix stuff"
git commit -m "changes"
```

### صيغة رسالة الالتزام المقترحة:
```
[نوع] - وصف قصير (50 حرف أو أقل)

وصف مفصل (اختياري):
- ماذا تم تغييره؟
- لماذا تم التغيير؟
- كيف يمكن اختبار هذا؟

أمثلة على الأنواع:
- feat: ميزة جديدة
- fix: إصلاح خطأ
- docs: تحديث التوثيق
- style: تنسيق الكود
- refactor: إعادة هيكلة بدون تغيير الوظيفة
- perf: تحسين الأداء
- test: إضافة اختبارات
```

**مثال واقعي:**
```bash
git commit -m "feat: Add dark mode toggle to settings

- Create DarkModeToggle component
- Update theme context to support dark/light modes
- Add styling for dark mode
- Test on mobile and desktop devices"
```

---

## 🔍 مراجعة الكود (Code Review Guidelines)

### للمبرمج الذي يرسل PR:

1. ✅ تأكد أن الكود يعمل بدون أخطاء
2. ✅ اتبع معايير الكود الموجودة
3. ✅ أضف comments في الأماكن المعقدة
4. ✅ اختبر على أكثر من جهاز/متصفح
5. ✅ اكتب وصفًا واضحًا للـ PR

### للمراجع (Reviewer):

1. ✅ اقرأ الكود بعناية
2. ✅ اختبر الميزة محليًا
3. ✅ ركز على:
   - **الوظيفة**: هل يعمل بشكل صحيح؟
   - **الأداء**: هل هناك تحسينات ممكنة؟
   - **الأمان**: هل هناك مشاكل أمان؟
   - **التوثيق**: هل الكود موثق بشكل كافي؟
4. ✅ اكتب comments بشكل محترم
5. ✅ إما وافق (Approve) أو اطلب تعديلات

---

## 📝 معايير الكود (Code Standards)

### اسم الملفات والمجلدات:
```
✅ Navbar.tsx              ❌ navbar.tsx
✅ contact.css             ❌ Contact.css
✅ useLanguage.ts          ❌ useLanguageHook.ts
✅ user-profile/           ❌ user_profile/
✅ get-tour-details        ❌ getTourDetails
```

### تسمية المتغيرات والدوال:
```javascript
// ✅ جيد
const userProfile = { name: "Ahmed" };
function calculateTotalPrice(items) { }
const isValidEmail = true;

// ❌ سيء
const up = { name: "Ahmed" };
function calc(i) { }
const valid = true;
```

### التعليقات:
```typescript
// ✅ تعليقات مفيدة
// Convert ISO date format to readable format
const formattedDate = new Date(isoDate).toLocaleDateString('ar-EG');

// ❌ تعليقات سيئة
// loop through items
for (let i = 0; i < items.length; i++) { }
```

---

## 🚀 خطوات الدفع إلى الإنتاج (Deployment)

```bash
# 1. تأكد أن جميع التغييرات في main
git checkout main
git pull origin main

# 2. بناء المشروع
npm run build

# 3. اختبر الإنتاج محليًا
npm run start

# 4. إذا كل شيء تمام، ادفع إلى الخادم
# (الخادم الخاص بك سيسحب آخر التحديثات تلقائيًا)
```

---

## 🔒 أفضل الممارسات الأمنية

### ❌ لا تضع في Git:
- كلمات مرور أو مفاتيح سرية
- ملفات `.env` المحلية
- ملفات حساسة

### ✅ افعل هذا بدلاً من ذلك:
- استخدم `.env.example` مع قيم مثال
- ضع الأسرار الفعلية محليًا فقط
- استخدم متغيرات البيئة للإنتاج

### مثال:
```bash
# ✅ مشاركة آمنة
.env.example: NEXT_PUBLIC_API_URL=http://localhost:3000

# محلي على جهازك
.env.local: NEXT_PUBLIC_API_URL=http://actual-api.com
```

---

## 📊 إحصائيات المشروع

```bash
# عدد الـ commits
git log --oneline | wc -l

# المساهمون
git shortlog -sn

# آخر التغييرات
git log --oneline -10

# الملفات المعدلة
git log --name-status --pretty=format:"%h" -1
```

---

## 🤝 التواصل بين أعضاء الفريق

### قنوات التواصل:
- **مشاكل الكود**: Comments في PR
- **النقاش العام**: Issues على GitHub
- **التنسيق**: WhatsApp أو Slack (حسب الفريق)

### نموذج تقرير المشكلة (Issue):
```markdown
## المشكلة
وصف واضح للمشكلة

## الخطوات لتكرار المشكلة
1. اذهب إلى...
2. اضغط على...
3. شاهد الخطأ

## السلوك المتوقع
ماذا يجب أن يحدث؟

## الملفات المتأثرة
- src/components/Navbar.tsx
- src/app/globals.css

## معلومات البيئة
- Browser: Chrome 120
- OS: Windows 11
- Node version: v18.0.0
```

---

## 📅 جدول التحديثات الموصى به

- **يوميًا**: اسحب آخر التحديثات (`git pull`)
- **مرتين أسبوعيًا**: مراجعة جميع PRs المفتوحة
- **أسبوعيًا**: دمج التحديثات الجاهزة مع الإنتاج
- **شهريًا**: اجتماع لمراجعة التقدم

---

## ✅ قائمة التحقق قبل Push

قبل رفع الكود، تأكد من:

```
[ ] الكود يعمل بدون أخطاء
[ ] اختبرت جميع الحالات
[ ] لا توجد رسائل أخطاء في console
[ ] الكود متوافق مع معايير المشروع
[ ] أضفت comments إذا لزم الأمر
[ ] لم أرفع بيانات حساسة
[ ] رسالة الالتزام واضحة
[ ] سحبت آخر التحديثات (git pull)
```

---

## 🆘 مشاكل شائعة وحلولها

### المشكلة: Merge Conflict
```bash
# حل 1: من VS Code
# اضغط على "Accept Current Change" أو "Accept Incoming Change"

# حل 2: من Terminal
git mergetool

# بعدها
git add .
git commit -m "Resolve merge conflicts"
git push origin feature-branch
```

### المشكلة: نسيت إضافة ملف
```bash
# أضفه وعدّل آخر commit
git add forgotten-file.tsx
git commit --amend --no-edit
git push -f origin feature-branch
```

### المشكلة: بدأت على branch خاطئ
```bash
# انسخ التغييرات
git stash

# انتقل للفرع الصحيح
git checkout -b feature/correct-branch

# استعد التغييرات
git stash pop
```

---

**🎯 تذكّر: التعاون الجيد = مشروع أفضل!**

شارك المعرفة، واطلب المساعدة، وساعد الآخرين. 🤝
